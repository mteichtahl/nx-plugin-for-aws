---
title: "프리셋"
description: "@aws/nx-plugin 워크스페이스 프리셋."
---



import { FileTree } from '@astrojs/starlight/components';
import RunGenerator from '@components/run-generator.astro';
import GeneratorParameters from '@components/generator-parameters.astro';
import schema from '../../../../../../packages/nx-plugin/src/preset/schema.json';

TODO: 생성자에 대한 개요를 여기에 추가하세요.

## 사용 방법

### 프리셋 생성

두 가지 방법으로 프리셋을 생성할 수 있습니다:

<RunGenerator generator="preset" />

### 옵션

<GeneratorParameters schema={schema} />

## 생성자 출력 결과

생성자는 다음 프로젝트 파일들을 생성합니다:

<FileTree>
  - todo.txt 생성자가 만드는 내용으로 이 섹션을 업데이트하세요!
</FileTree>

## TODO: 추가 문서 작성!

다음과 같은 세부 사항을 설명하는 문서를 추가하세요:

- 생성된 프로젝트/코드 작업 방법
- 추가된 새로운 project.json 타겟들과 실행 방법 (NxCommands 컴포넌트 사용)
- 생성된 프로젝트/코드 작업 시 모범 사례

구체적인 예시는 기존 가이드를 참조하세요.