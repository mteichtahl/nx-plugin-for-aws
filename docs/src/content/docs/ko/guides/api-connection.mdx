---
title: "API 연결"
description: "API 연결에 대한 참조 문서"
---



import Astro from '@astrojs/react';
import { LinkCard, CardGrid, LinkButton } from '@astrojs/starlight/components';

이 생성기는 프로젝트를 API에 연결하는 데 사용됩니다. 소스 프로젝트(API를 호출할 프로젝트)와 타겟 프로젝트(API 프로젝트)를 선택하기만 하면, 이 생성기가 두 프로젝트의 통합을 처리합니다.

### 지원되는 연결 방식

API 연결 생성기는 다음 연결 방식을 지원합니다:

<CardGrid>
  <LinkCard
    title="React to tRPC"
    description="React 웹사이트에서 tRPC API 호출"
    href={`/nx-plugin-for-aws/${Astro.currentLocale || 'en'}/guides/api-connection/react-trpc`}
  />
  <LinkCard
    title="React to FastAPI"
    description="React 웹사이트에서 Python FastAPI 호출"
    href={`/nx-plugin-for-aws/${Astro.currentLocale || 'en'}/guides/api-connection/react-fastapi`}
  />
</CardGrid>