---
title: "AIダンジョンゲーム"
description: "@aws/nx-pluginを使用してAIパワードのダンジョン冒険ゲームを構築する方法のチュートリアル。"
---



import { Aside, Code, FileTree, Steps, Tabs, TabItem } from '@astrojs/starlight/components';
import { Image } from 'astro:assets';
import Link from '@components/link.astro';
import Drawer from '@components/drawer.astro';
import RunGenerator from '@components/run-generator.astro';
import NxCommands from '@components/nx-commands.astro';
import InstallCommand from '@components/install-command.astro';
import CreateNxWorkspaceCommand from '@components/create-nx-workspace-command.astro';
import dungeonAdventureArchitecturePng from '@assets/dungeon-game-architecture.png'
import dungeonAdventureErPng from '@assets/dungeon-adventure-er.png'
import baselineWebsitePng from '@assets/baseline-website.png'
import baselineGamePng from '@assets/baseline-game.png'
import nxGraphPng from '@assets/nx-graph.png'
import gameSelectPng from '@assets/game-select.png'
import gameConversationPng from '@assets/game-conversation.png'

## モジュール1: Monorepoのセットアップ

まず新しいmonorepoを作成します。任意のディレクトリで次のコマンドを実行してください：

<CreateNxWorkspaceCommand workspace="dungeon-adventure" />

これにより`dungeon-adventure`ディレクトリ内にNX monorepoがセットアップされ、vscodeで開くことができます。ファイル構造は以下のようになります：

<FileTree>
- .nx/
- .vscode/
- node_modules/
- packages/ サブプロジェクトが配置される場所
- .gitignore
- .npmrc
- .prettierignore
- .prettierrc
- nx.json Nx CLIとmonorepoのデフォルト設定
- package.json 全Node依存関係の定義
- pnpm-lock.yaml または使用パッケージマネージャーに応じてbun.lock, yarn.lock, package-lock.json
- pnpm-workspace.yaml pnpm使用時
- README.md
- tsconfig.base.json 全Nodeベースサブプロジェクトが継承する設定 
- tsconfig.json
</FileTree>

`@aws/nx-plugin`のコンポーネントをmonorepoに追加するには、`dungeon-adventure`monorepoのルートで次のコマンドを実行してdev dependencyとしてインストールします：

<InstallCommand dev pkg="@aws/nx-plugin" />

これで`@aws/nx-plugin`を使用してさまざまなサブプロジェクトを作成する準備が整いました。

<Aside type="tip">ジェネレータを実行する前に、すべての未ステージングファイルをGitにコミットするのがベストプラクティスです。これにより`git diff`で変更内容を確認できます</Aside>

### Game API

まずGame APIを作成します。tRPC APIの`GameApi`を作成するには以下の手順に従ってください：

<RunGenerator generator="ts#trpc-api" requiredParameters={{apiName:"GameApi"}} noInteractive />

<br />

ファイルツリーに新しいファイルが生成されます。

<Aside>
ルート`package.json`に`type`が`module`として設定され、`@aws/nx-plugin`が提供するNodeベースサブプロジェクトのデフォルトモジュールタイプがESMになります。詳細は<Link path="guides/typescript-project">ts#projectガイド</Link>を参照してください。
</Aside>

<Drawer title="ts#trpc-apiで更新されたファイル" trigger="詳細を確認するにはこちらをクリック">
`ts#trpc-api`ジェネレータで生成された主要ファイルの説明：

<FileTree>
- packages/
  - common/
    - constructs/
      - src/
        - app/ アプリ固有CDKコンストラクト
          - apis/
            - **game-api.ts** tRPC API作成用CDKコンストラクト
            - index.ts
            - ...
          - index.ts
        - core/ 汎用CDKコンストラクト
          - api/
            - rest-api.ts API Gateway Rest APIベースコンストラクト
            - trpc-utils.ts trpc API CDKユーティリティ
            - utils.ts APIコンストラクトユーティリティ
          - index.ts
          - runtime-config.ts
        - index.ts
      - project.json
      - ...
    - types/ 共有型
      - src/
        - index.ts
        - runtime-config.ts CDKとWebサイトで使用するインターフェース定義
      - project.json
      - ...
  - game-api/
    - backend/ tRPC実装コード
      - src/
        - client/ 機械間通信用クライアント
          - index.ts
          - sigv4.ts
        - middleware/ Powertools計装
          - error.ts
          - index.ts
          - logger.ts
          - metrics.ts
          - tracer.ts
        - procedures/ APIプロシージャ/ルート実装
          - **echo.ts**
        - index.ts
        - init.ts コンテキストとミドルウェア設定
        - local-server.ts ローカルtRPCサーバー用
        - **router.ts** 全プロシージャを定義するLambdaハンドラエントリーポイント
      - project.json
      - ...
    - schema/
      - src/
        - procedures/
          - **echo.ts**
        - index.ts
      - project.json
      - ...
- eslint.config.mjs
- vitest.workspace.ts
</FileTree>

主要ファイルの解説：

```ts {5,12}
// packages/game-api/backend/src/router.ts
import {
  awsLambdaRequestHandler,
  CreateAWSLambdaContextOptions,
} from '@trpc/server/adapters/aws-lambda';
import { echo } from './procedures/echo.js';
import { t } from './init.js';
import { APIGatewayProxyEventV2WithIAMAuthorizer } from 'aws-lambda';

export const router = t.router;

export const appRouter = router({
  echo,
});

export const handler = awsLambdaRequestHandler({
  router: appRouter,
  createContext: (
    ctx: CreateAWSLambdaContextOptions<APIGatewayProxyEventV2WithIAMAuthorizer>,
  ) => ctx,
});

export type AppRouter = typeof appRouter;
```
ルーターはtRPC APIのエントリーポイントを定義し、すべてのAPIメソッドを宣言する場所です。`echo`メソッドの実装は`./procedures/echo.ts`にあります。

```ts {2-5}
// packages/game-api/backend/src/procedures/echo.ts
import { publicProcedure } from '../init.js';
import {
  EchoInputSchema,
  EchoOutputSchema,
} from ':dungeon-adventure/game-api-schema';

export const echo = publicProcedure
  .input(EchoInputSchema)
  .output(EchoOutputSchema)
  .query((opts) => ({ result: opts.input.message }));
```

`echo`メソッドの実装で、入力/出力データ構造を厳密に型定義しています。`:dungeon-adventure/game-api-schema`プロジェクトからスキーマ定義をインポートしています。

<Aside type="tip">IDEにインポートエラーが表示される場合、バックエンドのtsconfig.jsonにTypeScript参照が設定されていないためです。ビルド時または`nx sync`実行時に動的に参照が作成されます。詳細は<Link path="guides/typescript-project#importing-your-library-code-inother-projects">TypeScriptガイド</Link>を参照</Aside>

```ts
// packages/game-api/schema/src/procedures/echo.ts
import { z } from 'zod';

export const EchoInputSchema = z.object({
  message: z.string(),
});

export type IEchoInput = z.TypeOf<typeof EchoInputSchema>;

export const EchoOutputSchema = z.object({
  result: z.string(),
});

export type IEchoOutput = z.TypeOf<typeof EchoOutputSchema>;
```

すべてのtRPCスキーマ定義は[Zod](https://zod.dev/)を使用して定義され、`z.TypeOf`構文でTypeScript型としてエクスポートされます。

```ts
// packages/common/constructs/src/app/apis/game-api.ts
// ...（CDKコンストラクトの実装詳細）
```

このCDKコンストラクトはGameApiを定義します。`defaultIntegrations`メソッドは、tRPC APIの各プロシージャに対してLambda関数を自動生成し、バンドル済み実装を指します。これにより`cdk synth`時にバンドリングが発生しません。

</Drawer>

### Story API

次にFast APIの`StoryApi`を作成します：

<RunGenerator generator="py#fast-api" requiredParameters={{name:"StoryApi"}} noInteractive />

ファイルツリーに新しいファイルが生成されます。
<Drawer title="py#fast-apiで更新されたファイル" trigger="詳細を確認するにはこちらをクリック">
`py#fast-api`ジェネレータで生成された主要ファイル：

<FileTree>
- .venv/ monorepo用仮想環境
- packages/
  - common/
    - constructs/
      - src/
        - app/
          - apis/
            - **story-api.ts** FastAPI用CDKコンストラクト
            - index.ts 更新済み
      - project.json 更新済み
    - types/
      - src/
        - **runtime-config.ts** 更新済み
  - story_api/
    - story_api/ Pythonモジュール
      - init.py Powertools/FastAPI設定
      - **main.py** Lambdaエントリーポイント
    - tests/
    - .python-version
    - project.json
    - pyproject.toml
- .python-version
- pyproject.toml
- uv.lock
</FileTree>

```ts
// packages/common/constructs/src/app/apis/story-api.ts
// ...（CDKコンストラクトの実装詳細）
```

このCDKコンストラクトはStoryApiを定義します。`defaultIntegrations`メソッドはFastAPIの各操作に対してLambda関数を自動生成します。

```diff lang="ts"
// packages/common/types/src/runtime-config.ts
export type ApiUrl = string;
export interface IRuntimeConfig {
  apis: {
    GameApi: ApiUrl;
+    StoryApi: ApiUrl;
  };
}
```

ジェネレータがAST変換を実行し、`IRuntimeConfig`定義を更新する例。`StoryApi`が追加され、フロントエンドで型安全性が保証されます。

```py
// packages/story_api/story_api/main.py
from .init import app, lambda_handler, tracer

handler = lambda_handler

@app.get("/")
@tracer.capture_method
def read_root():
    return {"Hello": "World"}
```

APIメソッドを定義する場所です。[Pydantic](https://docs.pydantic.dev/latest/)を使用して入出力の型安全性を確保できます。

</Drawer>

### Game UI: ウェブサイト

ゲーム操作用UIを作成します：

<RunGenerator generator="ts#cloudscape-website" requiredParameters={{name:"GameUI"}} noInteractive />

ファイルツリーに新しいファイルが生成されます。
<Drawer title="ts#cloudscape-websiteで更新されたファイル" trigger="詳細を確認するにはこちらをクリック">
主要ファイルの解説：

<FileTree>
- packages/
  - game-ui/
    - public/
    - src/
      - components/
        - AppLayout/ ページレイアウト
          - index.ts
          - navitems.ts
      - hooks/
        - useAppLayout.tsx
      - routes/ ファイルベースルーティング
        - index.tsx
        - __root.tsx
        - welcome/
          - **index.tsx**
        - config.ts
        - **main.tsx** Reactエントリーポイント
        - routeTree.gen.ts
        - styles.css
    - index.html
    - project.json
    - vite.config.ts
</FileTree>

```ts
// packages/common/constructs/src/app/static-websites/game-ui.ts
// ...（CDKコンストラクトの実装）
```

ViteベースUIのバンドルパスを設定したCDKコンストラクト。

```tsx
// packages/game-ui/src/main.tsx
// ...（Reactエントリーポイントの実装）
```

ファイルベースルーティングを管理するReactのエントリーポイント。

```tsx
// packages/game-ui/src/routes/welcome/index.tsx
// ...（ウェルカムページコンポーネント）
```

`/welcome`ルート用コンポーネント。

</Drawer>

### Game UI: 認証

Amazon Cognito認証を設定します：

<RunGenerator generator="ts#cloudscape-website#auth" requiredParameters={{cognitoDomain:"game-ui", project:"@dungeon-adventure/game-ui", allowSignup:true}} noInteractive />

ファイルツリーが更新されます。
<Drawer title="認証関連更新ファイル" trigger="詳細を確認するにはこちらをクリック">
主要な更新内容：

```diff lang="tsx"
// packages/game-ui/src/main.tsx
+import CognitoAuth from './components/CognitoAuth';
+import RuntimeConfigProvider from './components/RuntimeConfig';
// ...（認証プロバイダの追加）
```

`RuntimeConfigProvider`と`CognitoAuth`コンポーネントが追加され、Cognito認証が有効化されます。

</Drawer>

### Game UI: Story API接続

Story APIへの接続を設定します：

<RunGenerator generator="api-connection" requiredParameters={{sourceProject:"@dungeon-adventure/game-ui", targetProject:"dungeon_adventure.story_api"}} noInteractive />

ファイルツリーが更新されます。
<Drawer title="UI-FastAPI接続更新ファイル" trigger="詳細を確認するにはこちらをクリック">
主要な更新内容：

```tsx
// packages/game-ui/src/hooks/useStoryApiClient.tsx
// ...（StoryApiクライアントフックの実装）
```

署名付きリクエスト用フックとTanStack Query統合。

</Drawer>

### Game UI: Game API接続

Game APIへの接続を設定します：

<RunGenerator generator="api-connection" requiredParameters={{sourceProject:"@dungeon-adventure/game-ui", targetProject:"@dungeon-adventure/game-api-backend"}} noInteractive />

ファイルツリーが更新されます。
<Drawer title="UI-tRPC接続更新ファイル" trigger="詳細を確認するにはこちらをクリック">
主要な更新内容：

```tsx
// packages/game-ui/src/hooks/useGameApi.tsx
// ...（tRPC React Query統合）
```

tRPCのReact Query統合により、バックエンド変更がフロントエンドに即時反映されます。

</Drawer>

### Game UI: インフラストラクチャ

CDKインフラ用プロジェクトを作成します：

<RunGenerator generator="ts#infra" requiredParameters={{name:"infra"}} noInteractive />

ファイルツリーが更新されます。
<Drawer title="インフラ関連更新ファイル" trigger="詳細を確認するにはこちらをクリック">
主要ファイル：

```ts
// packages/infra/src/main.ts
// ...（CDKアプリケーションエントリーポイント）
```

`cfn-guard`を使用したインフラ検証の設定。

```ts
// packages/infra/src/stacks/application-stack.ts
// ...（CDKスタック実装）
```

</Drawer>

#### インフラストラクチャの更新

`application-stack.ts`を更新してコンストラクトをインスタンス化：

```diff lang="ts"
// ...（CDKコンストラクトのインスタンス化コード）
```

API操作ごとに個別のLambda関数が作成されます。

### コードのビルド

<Drawer title="Nxコマンド" trigger="初回ビルドの実行">
###### シングルvsマルチターゲット

`run-many`コマンドは複数プロジェクトのターゲットを実行します。依存関係を正しい順序で処理します。

<NxCommands commands={['run @dungeon-adventure/infra:build']} />

###### 依存関係の可視化

<NxCommands commands={['graph']} />
<br/>

<Image src={nxGraphPng} alt="nx-graph.png" width="800" height="600" />

###### キャッシュ

キャッシュを無効化する場合：

<NxCommands commands={['run @dungeon-adventure/infra:build --skip-nx-cache']} />
キャッシュクリア：

<NxCommands commands={['reset']} />

</Drawer>

<NxCommands commands={['run-many --target build --all']} />

ビルド完了後、生成ファイルは`dist/`フォルダに出力されます。クリーン時は`dist/`を削除します。

<Aside type="caution" title="Windowsビルドエラー">
<Drawer trigger="Windowsユーザーはこちらをクリック" title="Windowsビルド問題">
`cfn-guard`のWindows未サポート問題に対処：

```diff lang="ts"
// packages/infra/src/main.ts
-// cfn-guard関連コードを削除
+const app = new App();
```
</Drawer>
</Aside>

おめでとうございます！ダンジョンアドベンチャーゲームのコア実装に必要な全サブプロジェクトの作成が完了しました。🎉🎉🎉