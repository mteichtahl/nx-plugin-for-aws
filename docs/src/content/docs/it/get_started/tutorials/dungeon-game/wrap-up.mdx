---
title: "Gioco di Dungeon con IA"
description: "Una guida dettagliata su come costruire un gioco di avventura dungeon basato su IA utilizzando @aws/nx-plugin."
---



import { Aside, Code, FileTree, Steps, Tabs, TabItem } from '@astrojs/starlight/components';
import { Image } from 'astro:assets';
import Drawer from '@components/drawer.astro';
import Link from '@components/link.astro';
import RunGenerator from '@components/run-generator.astro';
import NxCommands from '@components/nx-commands.astro';
import InstallCommand from '@components/install-command.astro';
import dungeonAdventureArchitecturePng from '@assets/dungeon-game-architecture.png'
import dungeonAdventureErPng from '@assets/dungeon-adventure-er.png'
import baselineWebsitePng from '@assets/baseline-website.png'
import baselineGamePng from '@assets/baseline-game.png'
import nxGraphPng from '@assets/nx-graph.png'
import gameSelectPng from '@assets/game-select.png'
import gameConversationPng from '@assets/game-conversation.png'

## Conclusione

Complimenti! Hai creato un'avventura dungeon che utilizza molti dei generatori disponibili nel plugin `@aws/nx-plugin`. 🎉🎉🎉

## Prossimi passi

Ti consigliamo di provare ad estendere il codice con queste funzionalità:

1. Aggiungi il <Link path="guides/license">generatore di licenze</Link> per gestire automaticamente file LICENSE e intestazioni nel codice sorgente
2. <Link path="get_started/tutorials/contribute-generator">Contribuisci con un tuo generatore</Link> per semplificare l'aggiunta di nuove API tRPC
3. Aggiungi una nuova API `resetToAction` (usando il tuo generatore) in GameAPI per tornare indietro nel tempo ed eliminare azioni da un punto specifico. Aggiorna l'UI aggiungendo un pulsante sui bubble di conversazione per richiamare questa funzione
4. Esplora le varie guide disponibili

### Distruzione delle risorse

Per eliminare le risorse AWS create, esegui:

<NxCommands commands={['run @dungeon-adventure/infra:destroy --all']} />

Il comando chiederà quali stack eliminare, tipicamente `waf` e `dungeon-adventure-infra-sandbox`. Digita `Y` per confermare e Cloudformation procederà alla rimozione.